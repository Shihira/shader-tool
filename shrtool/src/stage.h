#ifndef STAGE_H_INCLUDED
#define STAGE_H_INCLUDED

/*
 * Stage is a representation of a render pipeline. An element of render in the
 * pipeline is called a render task, which collects any information that is
 * necessary in the render period.
 */

namespace shrtool {

class render_task {
};

}

#endif // STAGE_H_INCLUDED
